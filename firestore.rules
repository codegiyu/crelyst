rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * Default: deny all client reads/writes. The app uses Firebase Admin on the server.
     * Adjust if you add direct client Firestore access for public content.
     */
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
